<%= form_for(@claim) do |f| %>
  <% if @claim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@claim.errors.count, "error") %> prohibited this claim from being saved:</h2>

      <ul>
      <% @claim.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :claim_text %><br>
    <%= f.text_field :claim_text %>
  </div>
  <div class="field">
    <%= f.label :claim_status %><br>
    <%= f.text_field :claim_status %>
  </div>
  <div class="field">
    <%= f.label :claim_count %><br>
    <%= f.number_field :claim_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
